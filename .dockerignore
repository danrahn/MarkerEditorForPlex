# Similar to .gitignore, but ignores broader swaths
# of files not relevant to running the app itself

# Dockerfile takes care of npm install
node_modules

# VS Code envrionment
.vscode
.git

Backup
dist
Logs
Test
cache
SVG/Raw
npm-debug.log
config.json
